import React from "react";
import CambiarPassword from "@/components/cambiarPassword/CambiarPassword";

const CambiarPassRender = (token) => {
  return <CambiarPassword token={token} />;
};
export default CambiarPassRender;
